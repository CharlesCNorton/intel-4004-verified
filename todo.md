# todo

Unfinished work.

- The model is cross-validated against physical 4004 silicon. (The
  gate-level netlist half is done: `netlistval/` differentially tests
  the model against a switch-level simulation of the transistor netlist
  captured from Intel's released mask artwork. The silicon bench remains
  for the analog questions the CPU netlist cannot answer, chiefly the
  multi-line DCL read contention between 4002s.)
